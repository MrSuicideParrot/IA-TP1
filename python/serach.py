class Search:

    def __init__(self,code):
        self.code = code
        if code is 1: # dfs
            fila = []
            return

        if code is 2: # bfs
            fila = []
            dict = {}
            return

        if code is 3: # idfs
            fila = []
            return

        if code is 4: # gulosa
            fila = []
            return

        if code is 5: # istar
            fila = []
            return



    def addFila(self):
        i
